# OpenclawWorkspace Python Dependencies
#
# 必选依赖（CI 自检用）：
# selfcheck.py 仅使用 Python 标准库，无外部依赖。Python 3.10+ 即可。
#
# 可选依赖（GPU PDF 转换，CI 不安装）：
# magic-pdf         # PDF → Markdown 转换（需要 vLLM）
# torch>=2.0         # PyTorch（GPU）
# vllm>=0.20         # VLM 推理引擎（需要 CUDA）
# mineru-openai-server  # MinerU vLLM 服务端
#
# CI 环境安装方式：
#   python3 -m pip install -r requirements.txt
# 本地完整安装（含 GPU）：
#   参考 TOOLS.md 中的 MinerU 架构说明
